package com.atom.app.ui.motion;

/** UI states that drive the atom core's energy, glow, and status copy. */
public enum CoreState {
    IDLE,
    LISTENING,
    THINKING,
    RESPONDED,
    ERROR,
    OPERATING
}
